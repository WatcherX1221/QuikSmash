using UnityEngine;

public class SolarLunar : MonoBehaviour
{
    public bool isSolar;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        isSolar = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
